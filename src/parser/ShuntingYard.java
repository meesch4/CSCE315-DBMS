package parser;

import dbms.Condition;

/**
 * Contains static methods that given a string, returns a Condition object
 */
public class ShuntingYard {
    /**
     *  Given an text input, returns a Condition object after performing the shunting-yard algorithm
     *  Could also have a ParseTree object as a parameter rather than a String, which should save us on parsing
     */
    public static Condition evaulate(String input) { // Rename this?
        return null;
    }
}
